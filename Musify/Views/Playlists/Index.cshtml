@model List<Musify.Models.Playlist>
@{
    ViewBag.Title = "Index";
}

@Html.ActionLink("Create New Playlist", "Create")
<div class="container">
    <h2>My Playlists</h2>
    <div class="row">


        @foreach (var playlist in Model)
        {
            <div class="col-md-3">
                <div>
                    @Html.DisplayFor(modelplaylist => playlist.Name)
                </div>
                @if (playlist.PlaylistDetails.Count > 0)
                {
                    <div>
                        <img src="~/img/@Url.Content(playlist.PlaylistDetails[0].Song.Thumbnail)" class="songs-photo" />
                    </div>
                    <div>@playlist.PlaylistDetails.Count songs</div>
                }


            </div>
        }

    </div>


</div>

@section scripts
{






}